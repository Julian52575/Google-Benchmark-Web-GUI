version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - .env
      - ./backend/.env
    ports:
      - "${BACKEND_PORT}:8000"
    volumes:
      - ${BENCHMARK_DIR}:${BENCHMARK_DIR}
    command: >
      bash -c "
        uvicorn main:app --host 0.0.0.0 --port 8000
      "

  frontend:
    build: ./frontend
    env_file:
      - .env
      - ./frontend/.env
    ports:
      - "${FRONTEND_PORT}:80"
# Google benchmark Web GUI BACKEND

Simplest gui viewer and comparer for google benchmarks.    

## Configuration

`.env` exposes the following variables that you are free to update:

| name | usage | default |
| ---- | ----- | ------- |
| BENCHMARK_DIR | The parent directory of the benchmark results | `/var/benchmarksResults/` |
------------------------- 